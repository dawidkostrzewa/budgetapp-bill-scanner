import React from 'react';
import { ImagePickerComponent } from '../Components/ImagePicker';

// TODO: type
export const Home = ({ navigation }: any) => {
  return <ImagePickerComponent navigation={navigation} />;
};
